// ── Google Fonts — EB Garamond (display) + Inter ──
// Injected once at the top of the page tree via this component.
const FontLink = () => (
  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@300;400&family=Inter:wght@400;500;600&display=swap"
  />
)

export default FontLink
